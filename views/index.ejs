<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Rotating Cube</title>
  <style>
    body { margin: 0; overflow: hidden; }
    canvas { display: block; }
    #slug-display {
      position: absolute;
      top: 10px;
      left: 10px;
      color: white;
      font-family: sans-serif;
      font-size: 24px;
      background-color: rgba(0,0,0,0.5);
      padding: 10px;
      border-radius: 5px;
    }
  </style>
</head>
<body>
  <div id="slug-display">You visited: <%= slug %></div>
  <script type="module" src="/js/main.js"></script>
</body>
</html> 